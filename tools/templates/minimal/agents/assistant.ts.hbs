import { createAgent } from "@frontal-labs/vibe/agent"
import { createAnthropicProvider, createFakeProvider } from "@frontal-labs/vibe/model"

const provider = process.env.ANTHROPIC_API_KEY
  ? createAnthropicProvider()
  : createFakeProvider([{ content: [{ type: "text", text: "Hello!" }] }])

export default createAgent({ provider, system: "You are a helpful assistant." })
