# Formatting rules for the Vibe compiler crates. Stable-rustfmt options only, so
# `cargo fmt --check` works on the pinned stable toolchain (see rust-toolchain.toml).
edition = "2021"
max_width = 100
tab_spaces = 4
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
