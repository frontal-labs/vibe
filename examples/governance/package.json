{
  "name": "@example/governance",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Policy engine + guardTool: deny or require approval for tool calls",
  "scripts": {
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@frontal-labs/vibe": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
