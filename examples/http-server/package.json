{
  "name": "@example/http-server",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Serve an agent over HTTP + SSE",
  "scripts": {
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@frontal-labs/vibe": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
